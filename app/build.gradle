apply plugin: 'com.android.application'

android {
    compileSdkVersion 29
    buildToolsVersion "33.0.0"

    defaultConfig {
        applicationId "org.crazyit.gallery"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
